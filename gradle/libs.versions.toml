[versions]
# PewPewCubes
pewpewcubes = "1.0.0-beta1"

# Common
minecraft-range = "[1.19.2]"
minecraft = "1.19.2"
yarn = "1.19.2+build.28"

# GeckoLib
geckolib = "3.1.40"

# Fabric
fabric = "0.14.19"
fabric-range = "0.14"
fabric-api = "0.76.0+1.19.2"
fabric-api-range = "0.76.0+1.19.2"

# Dependencies
mixin = "0.8.7"

# Plugins
plugin-loom = "1.9-SNAPSHOT"

[libraries]
# Mixin
mixin = { group = "org.spongepowered", name = "mixin", version.ref = "mixin" }

# Platform
fabric = { group = "net.fabricmc", name = "fabric-loader", version.ref = "fabric" }
fabric-api = { group = "net.fabricmc.fabric-api", name = "fabric-api", version.ref = "fabric-api" }
minecraft = { group = "com.mojang", name = "minecraft", version.ref = "minecraft" }

# Other
geckolib = { group = "software.bernie.geckolib", name="geckolib-fabric-1.19", version.ref="geckolib"}

[plugins]

# Platform
loom = { id = "fabric-loom", version.ref = "plugin-loom" }